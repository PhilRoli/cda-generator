# CDA-Übung v1.1 Feature-Set — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sechs Features hinzufügen: Version-Badge + Changelog, Bundesländer-Adressen in Faker, Ambulant/Stationär-Encountertyp (inkl. CDA-XML), öffentliche Cloud-Szenarien aller Nutzer, Bundesland-angepasste Krankenhausliste mit Auto-Fill.

**Architecture:** Vanilla-JS Frontend (ES-Module, kein Build-Tool) + Spring Boot 3 Backend (SQLite). Frontend-Änderungen betreffen `index.html`, `css/form.css` und `js/`-Module. Backend-Änderungen betreffen `ScenarioService` und `ScenarioController`. Dist-Build durch `scripts/build-dist.sh` am Ende.

**Tech Stack:** Vanilla JS (ES Modules), HTML5 `<dialog>`, Java 21 / Spring Boot 3.3, SQLite, JUnit 5 / Spring MockMvc, Maven

---

## File Map

| File | Action | Responsibility |
|------|--------|---------------|
| `index.html` | Modify | Version-Badge, Changelog-Dialog, Encounter-Type-Select, Krankenhaus-Selektoren, Cloud-Show-All-Checkbox |
| `css/form.css` | Modify | Styles für Version-Badge, Changelog-Dialog |
| `js/app.js` | Modify | APP_VERSION-Konstante, Changelog-Handler, encounter.type-Default, All-Scenarios-Toggle, Hospital-Selector-Setup |
| `js/cda-builder.js` | Modify | `renderEncompassingEncounter` dynamischer Code, `defaultBrieftext` ambulant/stationär |
| `js/faker.js` | Modify | ADRESSEN um alle 9 Bundesländer erweitern |
| `js/hospitals.js` | Create | HOSPITALS_BY_BUNDESLAND Datei (9 Bundesländer, je 3–8 Krankenhäuser mit Adressdaten) |
| `src/main/java/at/rolinek/cda/scenario/ScenarioService.java` | Modify | `listAll()` und `getByIdPublic()` ohne Auth-Check |
| `src/main/java/at/rolinek/cda/api/ScenarioController.java` | Modify | `GET /api/scenarios/all` (public), username optional in `GET /api/scenarios/{id}` |
| `src/test/java/at/rolinek/cda/api/ScenarioControllerPublicTest.java` | Create | Tests für neue public Endpoints |

---

## Task 1: Version Badge + Changelog

**Files:**
- Modify: `index.html`
- Modify: `css/form.css`
- Modify: `js/app.js`

- [ ] **Step 1: CSS-Styles für Version-Badge und Changelog-Dialog hinzufügen**

Am Ende von `css/form.css` einfügen:

```css
.header-version-badge {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    height: 32px;
    padding: 0 10px;
    border: 1px solid var(--border);
    border-radius: 6px;
    background: var(--panel);
    color: var(--muted);
    font-size: 12px;
    font-weight: 500;
    cursor: pointer;
    user-select: none;
}

.header-version-badge:hover {
    background: #f0f0f3;
}

dialog.changelog-dialog {
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 24px;
    max-width: 540px;
    width: 90%;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.12);
}

dialog.changelog-dialog::backdrop {
    background: rgba(0, 0, 0, 0.3);
}

dialog.changelog-dialog h2 {
    margin-top: 0;
}

.changelog-entry {
    margin-bottom: 20px;
}

.changelog-entry h3 {
    font-size: 14px;
    font-weight: 600;
    margin: 0 0 8px;
    color: var(--accent);
}

.changelog-entry ul {
    margin: 0;
    padding-left: 18px;
    font-size: 13px;
    line-height: 1.7;
    color: var(--text);
}
```

- [ ] **Step 2: Version-Badge und Changelog-Dialog in index.html einfügen**

Im `<header>` nach dem E-Mail-Button den Badge einfügen:

```html
<!-- Bestehend: -->
<a class="header-email-btn" href="mailto:...">📧 E-Mail</a>
<!-- NEU danach: -->
<span id="version-badge" class="header-version-badge"></span>
```

Direkt vor `</body>` den Changelog-Dialog einfügen:

```html
<dialog id="changelog-dialog" class="changelog-dialog">
    <h2>Changelog</h2>
    <div class="changelog-entry">
        <h3>v1.1.0 — April 2026</h3>
        <ul>
            <li>Versionsanzeige und Changelog</li>
            <li>Adressdaten für alle österreichischen Bundesländer</li>
            <li>Ambulant / Stationär Besuchstyp (wirkt auf CDA-XML)</li>
            <li>Alle Cloud-Szenarien öffentlich einsehbar (mit Benutzernamen)</li>
            <li>Bundesland-angepasste Krankenhausliste mit Auto-Fill</li>
        </ul>
    </div>
    <div class="changelog-entry">
        <h3>v1.0.0 — März 2026</h3>
        <ul>
            <li>Formulargestützter CDA Entlassungsbrief-Ersteller</li>
            <li>PDF- und XML-Generierung mit ELGA-Stylesheet und Wasserzeichen</li>
            <li>Lokale Szenario-Verwaltung (JSON-Datei)</li>
            <li>Cloud-Szenario-Verwaltung (eigene Szenarien)</li>
            <li>Zufällige Patientenstammdaten (Faker für Österreich)</li>
            <li>Quick-Add für Diagnosen, Medikation, Allergien, Risikofaktoren</li>
            <li>Patientenverfügungs-Status mit bedingten Feldern</li>
        </ul>
    </div>
    <button class="btn" id="changelog-close">Schließen</button>
</dialog>
```

- [ ] **Step 3: APP_VERSION-Konstante und Badge-Logik in js/app.js**

Am Anfang von `js/app.js`, nach den Imports, einfügen:

```js
const APP_VERSION = '1.1.0';
```

In der `init()`-Funktion am Ende, vor der letzten schließenden Klammer, einfügen:

```js
const badge = document.getElementById('version-badge');
badge.textContent = `v${APP_VERSION}`;
badge.addEventListener('click', () => document.getElementById('changelog-dialog').showModal());
document.getElementById('changelog-close').addEventListener('click', () =>
    document.getElementById('changelog-dialog').close()
);
```

- [ ] **Step 4: Browser-Test**

Dev-Server starten (`npm start` oder direkt den Java-Server per `./mvnw spring-boot:run`) und manuell prüfen:
- Badge `v1.1.0` erscheint rechts neben dem E-Mail-Button
- Klick öffnet Changelog-Modal
- „Schließen" schließt es
- Klick auf Backdrop schließt NICHT (natives `<dialog>`-Verhalten — kein Event-Listener nötig, das ist korrekt)

- [ ] **Step 5: Commit**

```bash
git add index.html css/form.css js/app.js
git commit -m "feat: add version badge and changelog modal (v1.1.0)"
```

---

## Task 2: Faker — Adressen für alle 9 Bundesländer

**Files:**
- Modify: `js/faker.js`

- [ ] **Step 1: ADRESSEN-Array in js/faker.js durch vollständige Version ersetzen**

Den gesamten bisherigen `const ADRESSEN = [...]`-Block (Zeilen 99–115) ersetzen durch:

```js
const ADRESSEN = [
    // Wien
    { street: 'Mariahilfer Straße', plz: '1060', city: 'Wien' },
    { street: 'Währinger Straße', plz: '1180', city: 'Wien' },
    { street: 'Simmeringer Hauptstraße', plz: '1110', city: 'Wien' },
    { street: 'Floridsdorfer Hauptstraße', plz: '1210', city: 'Wien' },
    // Niederösterreich
    { street: 'Kremser Gasse', plz: '3500', city: 'Krems an der Donau' },
    { street: 'Rathausplatz', plz: '3100', city: 'St. Pölten' },
    { street: 'Hauptstraße', plz: '2700', city: 'Wiener Neustadt' },
    { street: 'Badener Straße', plz: '2380', city: 'Perchtoldsdorf' },
    // Oberösterreich
    { street: 'Landstraße', plz: '4020', city: 'Linz' },
    { street: 'Stadtplatz', plz: '4600', city: 'Wels' },
    { street: 'Stadtplatz', plz: '4400', city: 'Steyr' },
    { street: 'Bahnhofstraße', plz: '4210', city: 'Gallneukirchen' },
    // Salzburg
    { street: 'Linzer Bundesstraße', plz: '5023', city: 'Salzburg' },
    { street: 'Münchner Bundesstraße', plz: '5020', city: 'Salzburg' },
    { street: 'Alpenstraße', plz: '5020', city: 'Salzburg' },
    { street: 'Marktplatz', plz: '5400', city: 'Hallein' },
    { street: 'Bahnhofstraße', plz: '5500', city: 'Bischofshofen' },
    // Steiermark
    { street: 'Herrengasse', plz: '8010', city: 'Graz' },
    { street: 'Annenstraße', plz: '8020', city: 'Graz' },
    { street: 'Hauptplatz', plz: '8530', city: 'Deutschlandsberg' },
    { street: 'Hauptstraße', plz: '8605', city: 'Kapfenberg' },
    // Tirol
    { street: 'Maria-Theresien-Straße', plz: '6020', city: 'Innsbruck' },
    { street: 'Stadtplatz', plz: '6130', city: 'Schwaz' },
    { street: 'Unterer Stadtplatz', plz: '6330', city: 'Kufstein' },
    { street: 'Bahnhofstraße', plz: '6300', city: 'Wörgl' },
    // Vorarlberg
    { street: 'Rathausstraße', plz: '6900', city: 'Bregenz' },
    { street: 'Marktstraße', plz: '6850', city: 'Dornbirn' },
    { street: 'Marktgasse', plz: '6800', city: 'Feldkirch' },
    { street: 'Hauptstraße', plz: '6830', city: 'Rankweil' },
    // Kärnten
    { street: 'Alter Platz', plz: '9020', city: 'Klagenfurt am Wörthersee' },
    { street: 'Hauptplatz', plz: '9500', city: 'Villach' },
    { street: 'Hauptstraße', plz: '9400', city: 'Wolfsberg' },
    { street: 'Bahnhofstraße', plz: '9300', city: 'St. Veit an der Glan' },
    // Burgenland
    { street: 'Esterházystraße', plz: '7000', city: 'Eisenstadt' },
    { street: 'Hauptstraße', plz: '7400', city: 'Oberwart' },
    { street: 'Hauptplatz', plz: '7350', city: 'Oberpullendorf' },
    { street: 'Hauptstraße', plz: '7100', city: 'Neusiedl am See' },
];
```

- [ ] **Step 2: Browser-Test**

„🎲 Zufällige Stammdaten" mehrfach klicken → Adressen aus verschiedenen Bundesländern erscheinen.

- [ ] **Step 3: Commit**

```bash
git add js/faker.js
git commit -m "feat: extend patient address faker to cover all 9 Austrian states"
```

---

## Task 3: Ambulant / Stationär

**Files:**
- Modify: `index.html`
- Modify: `js/app.js`
- Modify: `js/cda-builder.js`

- [ ] **Step 1: encounter.type-Default in defaultState() hinzufügen (js/app.js)**

Im `defaultState()`-Return-Objekt, im `encounter:`-Block, `type: 'IMP'` ergänzen:

```js
encounter: {
    admissionDate: isoLocal(admission),
    dischargeDate: isoLocal(discharge),
    ward: 'Unfallchirurgie, Station 3B',
    caseId: '',
    type: 'IMP',
},
```

- [ ] **Step 2: Besuchstyp-Select in index.html einfügen**

Im Aufenthalt-Abschnitt, direkt nach `<h2 style="margin-top: 20px">Aufenthalt</h2>` und **vor** dem bestehenden `<div class="grid-3">`, einfügen:

```html
<div class="grid-2">
    <label>
        <span class="label-text">Besuchstyp</span>
        <select data-bind="encounter.type">
            <option value="IMP">Stationär</option>
            <option value="AMB">Ambulant</option>
        </select>
    </label>
</div>
```

- [ ] **Step 3: renderEncompassingEncounter in cda-builder.js anpassen**

Die Funktion `renderEncompassingEncounter` (beginnt bei Zeile 135) ersetzen:

```js
function renderEncompassingEncounter(encounter, organization) {
    if (!encounter || (!encounter.admissionDate && !encounter.dischargeDate)) return '';
    const low = encounter.admissionDate ? `<low value="${toHl7Time(encounter.admissionDate)}"/>` : '';
    const high = encounter.dischargeDate ? `<high value="${toHl7Time(encounter.dischargeDate)}"/>` : '';
    const type = encounter.type || 'IMP';
    const encounterCode = type === 'AMB'
        ? `<code code="AMB" codeSystem="2.16.840.1.113883.5.4" codeSystemName="HL7:ActCode" displayName="ambulatory"/>`
        : `<code code="IMP" codeSystem="2.16.840.1.113883.5.4" codeSystemName="HL7:ActCode" displayName="inpatient encounter"/>`;
    return `<componentOf>
<encompassingEncounter>
<id root="1.2.40.0.34.99.111.1.5" extension="${escapeXml(encounter.caseId || uuid())}"/>
${encounterCode}
<effectiveTime>
${low}
${high}
</effectiveTime>
<location>
<healthCareFacility>
<id root="1.2.40.0.34.99.111.1.6"/>
<code code="HOSP" codeSystem="2.16.840.1.113883.5.111" codeSystemName="HL7:RoleCode" displayName="Hospital"/>
<location>
<name>${escapeXml(encounter.ward || organization.name)}</name>
</location>
<serviceProviderOrganization>
${renderOrganizationBlock(organization)}
</serviceProviderOrganization>
</healthCareFacility>
</location>
</encompassingEncounter>
</componentOf>`;
}
```

- [ ] **Step 4: defaultBrieftext in cda-builder.js anpassen**

Die Funktion `defaultBrieftext` (beginnt bei Zeile 185) ersetzen:

```js
function defaultBrieftext(state) {
    const female = state.patient?.gender === 'F';
    const accusative = female ? 'die oben genannte Patientin' : 'den oben genannten Patienten';
    const relative = female ? 'die sich' : 'der sich';
    const adm = formatGermanDateTime(state.encounter?.admissionDate);
    const dis = formatGermanDateTime(state.encounter?.dischargeDate);
    const stayPart = adm && dis ? `vom ${adm} bis ${dis}` : adm ? `seit ${adm}` : dis ? `bis ${dis}` : '';
    const treatment = (state.encounter?.type || 'IMP') === 'AMB'
        ? 'ambulanter Behandlung'
        : 'stationärer Behandlung';
    return `Sehr geehrte Frau Kollegin, sehr geehrter Herr Kollege,\n\nwir berichten Ihnen über ${accusative}, ${relative} ${stayPart} in unserer ${treatment} befand.`;
}
```

- [ ] **Step 5: Browser-Test**

- Besuchstyp auf „Ambulant" setzen, XML herunterladen
- In der XML prüfen: `<code code="AMB" ... displayName="ambulatory"/>` in `<encompassingEncounter>`
- Wieder auf „Stationär" stellen, XML herunterladen → `code="IMP"` erscheint
- Brieftext-Sektion leeren, XML herunterladen → Text enthält „ambulanter Behandlung" bzw. „stationärer Behandlung"

- [ ] **Step 6: Commit**

```bash
git add index.html js/app.js js/cda-builder.js
git commit -m "feat: add ambulant/stationaer encounter type with CDA XML support"
```

---

## Task 4: Backend — Öffentliche Szenarien-Endpoints

**Files:**
- Modify: `src/main/java/at/rolinek/cda/scenario/ScenarioService.java`
- Modify: `src/main/java/at/rolinek/cda/api/ScenarioController.java`
- Create: `src/test/java/at/rolinek/cda/api/ScenarioControllerPublicTest.java`

- [ ] **Step 1: Test-Datei erstellen (failing)**

Verzeichnis anlegen und Test schreiben:

```bash
mkdir -p src/test/java/at/rolinek/cda/api
```

`src/test/java/at/rolinek/cda/api/ScenarioControllerPublicTest.java`:

```java
package at.rolinek.cda.api;

import at.rolinek.cda.scenario.ScenarioRecord;
import at.rolinek.cda.scenario.ScenarioService;
import com.fasterxml.jackson.databind.ObjectMapper;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.WebMvcTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.test.web.servlet.MockMvc;

import java.util.List;

import static org.mockito.BDDMockito.given;
import static org.mockito.ArgumentMatchers.any;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.*;

@WebMvcTest(ScenarioController.class)
class ScenarioControllerPublicTest {

    @Autowired
    MockMvc mvc;

    @MockBean
    ScenarioService scenarioService;

    private static final ScenarioRecord SAMPLE = new ScenarioRecord(
        "id1", "testuser", "Szenario A", "{}", "2026-01-01T00:00:00Z", "2026-01-01T00:00:00Z"
    );

    @Test
    void listAll_returnsAllScenariosWithoutAuth() throws Exception {
        given(scenarioService.listAll()).willReturn(List.of(SAMPLE));

        mvc.perform(get("/api/scenarios/all"))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$[0].username").value("testuser"))
            .andExpect(jsonPath("$[0].title").value("Szenario A"));
    }

    @Test
    void getById_withoutUsername_returnsPublicScenario() throws Exception {
        given(scenarioService.getByIdPublic("id1")).willReturn(SAMPLE);
        given(scenarioService.payloadToJson(any())).willReturn(new ObjectMapper().readTree("{}"));

        mvc.perform(get("/api/scenarios/id1"))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.username").value("testuser"))
            .andExpect(jsonPath("$.title").value("Szenario A"));
    }
}
```

- [ ] **Step 2: Test ausführen — erwartet FAIL**

```bash
./mvnw test -pl . -Dtest=ScenarioControllerPublicTest 2>&1 | tail -20
```

Erwartetes Ergebnis: Compilation error / Test failure wegen fehlender `listAll()`- und `getByIdPublic()`-Methoden in `ScenarioService`.

- [ ] **Step 3: ScenarioService um öffentliche Methoden erweitern**

In `src/main/java/at/rolinek/cda/scenario/ScenarioService.java` nach der `listAllForAdmin`-Methode einfügen:

```java
public List<ScenarioRecord> listAll() {
    return repository.listAll();
}

public ScenarioRecord getByIdPublic(String id) {
    return repository.findById(id)
        .orElseThrow(() -> new ResponseStatusException(HttpStatus.NOT_FOUND, "Szenario nicht gefunden."));
}
```

- [ ] **Step 4: ScenarioController anpassen**

In `src/main/java/at/rolinek/cda/api/ScenarioController.java`:

**a)** Den Import `jakarta.validation.constraints.NotBlank` bleibt (wird noch vom List-Endpoint genutzt).

**b)** Den bestehenden `GET /scenarios/{id}` Endpoint ersetzen:

```java
@GetMapping("/scenarios/{id}")
public ScenarioDetailResponse get(
    @PathVariable("id") String id,
    @RequestParam(value = "username", required = false) String username
) {
    ScenarioRecord record = (username != null && !username.isBlank())
        ? scenarioService.getByIdForUser(id, username)
        : scenarioService.getByIdPublic(id);
    return ScenarioDetailResponse.from(record, scenarioService.payloadToJson(record));
}
```

**c)** Neuen Endpoint **vor** `GET /scenarios/{id}` einfügen (damit Spring das Literal `all` vor der PathVariable erkennt):

```java
@GetMapping("/scenarios/all")
public List<ScenarioSummaryResponse> listAll() {
    return scenarioService.listAll().stream()
        .map(ScenarioSummaryResponse::from)
        .toList();
}
```

- [ ] **Step 5: Tests ausführen — erwartet PASS**

```bash
./mvnw test -pl . -Dtest=ScenarioControllerPublicTest 2>&1 | tail -20
```

Erwartetes Ergebnis: `Tests run: 2, Failures: 0, Errors: 0`

- [ ] **Step 6: Alle Tests ausführen**

```bash
./mvnw test 2>&1 | tail -10
```

Erwartetes Ergebnis: `BUILD SUCCESS`

- [ ] **Step 7: Commit**

```bash
git add src/
git commit -m "feat: add public GET /api/scenarios/all and public scenario fetch by id"
```

---

## Task 5: Frontend — Alle Cloud-Szenarien anzeigen

**Files:**
- Modify: `index.html`
- Modify: `js/app.js`

- [ ] **Step 1: Checkbox in index.html einfügen**

Im Cloud-Aktionen-Block (`id="scenario-cloud-actions"`), direkt nach der öffnenden `<div>`-Klammer und **vor** der ersten `.scenario-row`, einfügen:

```html
<label class="scenario-row" style="gap: 8px; align-items: center; cursor: pointer;">
    <input type="checkbox" id="cloud-show-all" />
    <span>Alle Szenarien aller Benutzer anzeigen</span>
</label>
```

- [ ] **Step 2: Hilfsfunktion isShowAll() in js/app.js hinzufügen**

Direkt nach `function getCloudUsername()` einfügen:

```js
function isShowAll() {
    return document.getElementById('cloud-show-all')?.checked ?? false;
}
```

- [ ] **Step 3: refreshCloudScenarios() anpassen**

Die bestehende Funktion `refreshCloudScenarios` ersetzen:

```js
async function refreshCloudScenarios(options = {}) {
    const { silent = false } = options;

    if (isShowAll()) {
        const list = await apiJson('/api/scenarios/all');
        cloudScenarios = list;
        renderCloudScenarioSelect();
        if (!silent) setStatus(`Cloud-Liste aktualisiert: ${cloudScenarios.length} Szenario(s) von allen Benutzern.`);
        return;
    }

    const username = getCloudUsername();
    if (!username) {
        cloudScenarios = [];
        renderCloudScenarioSelect();
        if (!silent) setStatus('Bitte zuerst einen Benutzernamen für Cloud-Szenarien eingeben.');
        return;
    }
    const list = await apiJson(`/api/scenarios?username=${encodeURIComponent(username)}`);
    cloudScenarios = list;
    renderCloudScenarioSelect();
    if (!silent) setStatus(`Cloud-Liste aktualisiert: ${cloudScenarios.length} Szenario(s).`);
}
```

- [ ] **Step 4: renderCloudScenarioSelect() anpassen — Username-Präfix anzeigen**

Die bestehende Funktion `renderCloudScenarioSelect` ersetzen:

```js
function renderCloudScenarioSelect() {
    const select = document.getElementById('cloud-scenario-select');
    if (!select) return;
    const keepId = selectedCloudScenarioId;
    const showAll = isShowAll();
    select.innerHTML = '';

    if (!cloudScenarios.length) {
        const option = document.createElement('option');
        option.value = '';
        option.textContent = 'Keine Cloud-Szenarien vorhanden';
        select.appendChild(option);
        selectedCloudScenarioId = null;
        return;
    }

    cloudScenarios.forEach((scenario) => {
        const option = document.createElement('option');
        option.value = scenario.id;
        const prefix = showAll ? `[${scenario.username}] ` : '';
        option.textContent = `${prefix}${scenario.title} · ${scenario.updatedAt}`;
        if (keepId && keepId === scenario.id) option.selected = true;
        select.appendChild(option);
    });

    selectedCloudScenarioId = select.value || cloudScenarios[0].id;
    if (selectedCloudScenarioId) select.value = selectedCloudScenarioId;
}
```

- [ ] **Step 5: loadCloudScenario() anpassen — kein Username-Zwang bei "Alle"**

Die bestehende Funktion `loadCloudScenario` ersetzen:

```js
async function loadCloudScenario() {
    if (!selectedCloudScenarioId) {
        setStatus('Bitte zuerst ein Cloud-Szenario auswählen.');
        return;
    }

    let url;
    if (isShowAll()) {
        url = `/api/scenarios/${encodeURIComponent(selectedCloudScenarioId)}`;
    } else {
        const username = getCloudUsername();
        if (!username) {
            setStatus('Bitte zuerst einen Benutzernamen eingeben.');
            return;
        }
        url = `/api/scenarios/${encodeURIComponent(selectedCloudScenarioId)}?username=${encodeURIComponent(username)}`;
    }

    const detail = await apiJson(url);
    state = Object.assign(defaultState(), detail.state || {});
    saveState();
    rebindAll();
    setStatus(`Cloud-Szenario geladen: ${detail.title}`);
}
```

- [ ] **Step 6: Checkbox-Event-Listener in setupScenarioManager() registrieren**

In der Funktion `setupScenarioManager()`, nach dem `select.addEventListener('change', ...)`-Block, einfügen:

```js
document.getElementById('cloud-show-all').addEventListener('change', async () => {
    try {
        await refreshCloudScenarios({ silent: false });
    } catch (err) {
        setStatus(`Cloud-Refresh fehlgeschlagen: ${err.message}`);
    }
});
```

- [ ] **Step 7: Browser-Test**

- Cloud-Modus aktivieren
- Checkbox „Alle Szenarien" anhaken → Liste lädt ohne Username-Eingabe, Einträge zeigen `[username] Titel`
- Checkbox deaktivieren → eigener-Username-Modus kehrt zurück
- Fremdes Szenario laden → State wird korrekt befüllt

- [ ] **Step 8: Commit**

```bash
git add index.html js/app.js
git commit -m "feat: add public all-scenarios toggle in cloud panel with creator username display"
```

---

## Task 6: Krankenhausdaten-Datei erstellen

**Files:**
- Create: `js/hospitals.js`

- [ ] **Step 1: js/hospitals.js erstellen**

```js
// Kuratierte Liste österreichischer Krankenhäuser, gegliedert nach Bundesland.
// Dient dem Bundesland-Quick-Select im Krankenhaus-Formular.

export const HOSPITALS_BY_BUNDESLAND = {
    W: [
        { name: 'AKH Wien — Allgemeines Krankenhaus der Stadt Wien', phone: '+43 1 40400 0', street: 'Währinger Gürtel', houseNumber: '18-20', postalCode: '1090', city: 'Wien' },
        { name: 'Klinik Floridsdorf', phone: '+43 1 27700 0', street: 'Brünner Straße', houseNumber: '68', postalCode: '1210', city: 'Wien' },
        { name: 'Klinik Favoriten', phone: '+43 1 60191 0', street: 'Kundratstraße', houseNumber: '3', postalCode: '1100', city: 'Wien' },
        { name: 'Krankenhaus Hietzing', phone: '+43 1 80110 0', street: 'Wolkersbergenstraße', houseNumber: '1', postalCode: '1130', city: 'Wien' },
        { name: 'Krankenhaus der Barmherzigen Brüder Wien', phone: '+43 1 211 21 0', street: 'Große Mohrengasse', houseNumber: '9', postalCode: '1020', city: 'Wien' },
    ],
    NOE: [
        { name: 'Universitätsklinikum St. Pölten', phone: '+43 2742 9009 0', street: 'Dunant-Platz', houseNumber: '1', postalCode: '3100', city: 'St. Pölten' },
        { name: 'Landesklinikum Wiener Neustadt', phone: '+43 2622 321 0', street: 'Corvinusring', houseNumber: '3-5', postalCode: '2700', city: 'Wiener Neustadt' },
        { name: 'Landesklinikum Amstetten', phone: '+43 7472 604 0', street: 'Ybbsstraße', houseNumber: '8', postalCode: '3300', city: 'Amstetten' },
        { name: 'Landesklinikum Krems', phone: '+43 2732 9004 0', street: 'Mitterweg', houseNumber: '10', postalCode: '3500', city: 'Krems an der Donau' },
    ],
    OOE: [
        { name: 'Kepler Universitätsklinikum Linz', phone: '+43 5 7680 81', street: 'Krankenhausstraße', houseNumber: '9', postalCode: '4021', city: 'Linz' },
        { name: 'Ordensklinikum Linz Barmherzige Schwestern', phone: '+43 732 7677 0', street: 'Seilerstätte', houseNumber: '4', postalCode: '4010', city: 'Linz' },
        { name: 'Salzkammergut Klinikum Vöcklabruck', phone: '+43 5 0554 66', street: 'Dr.-Wilhelm-Bock-Straße', houseNumber: '1', postalCode: '4840', city: 'Vöcklabruck' },
        { name: 'Pyhrn-Eisenwurzen Klinikum Steyr', phone: '+43 5 0554 63', street: 'Sierningerstraße', houseNumber: '170', postalCode: '4400', city: 'Steyr' },
    ],
    S: [
        { name: 'Universitätsklinikum Salzburg Landeskrankenhaus', phone: '+43(0)57255', street: 'Müllner Hauptstraße', houseNumber: '48', postalCode: '5020', city: 'Salzburg' },
        { name: 'Kardinal Schwarzenberg Klinikum', phone: '+43 6452 7110 0', street: 'Kardinal-Schwarzenberg-Straße', houseNumber: '2-6', postalCode: '5620', city: 'Schwarzach im Pongau' },
        { name: 'Tauernklinikum Zell am See', phone: '+43 6542 777 0', street: 'Paracelsusstraße', houseNumber: '8', postalCode: '5700', city: 'Zell am See' },
        { name: 'Klinikum Mittersill', phone: '+43 6562 6161 0', street: 'Felberstraße', houseNumber: '33', postalCode: '5730', city: 'Mittersill' },
    ],
    ST: [
        { name: 'LKH-Univ. Klinikum Graz', phone: '+43 316 385 0', street: 'Auenbruggerplatz', houseNumber: '1', postalCode: '8036', city: 'Graz' },
        { name: 'LKH Hochsteiermark Standort Leoben', phone: '+43 5 7200 0', street: 'Vordernberger Straße', houseNumber: '42', postalCode: '8700', city: 'Leoben' },
        { name: 'Krankenhaus der Elisabethinen Graz', phone: '+43 316 7063 0', street: 'Elisabethinergasse', houseNumber: '14', postalCode: '8020', city: 'Graz' },
        { name: 'LKH Hochsteiermark Standort Bruck an der Mur', phone: '+43 5 7200 0', street: 'Tragösser Straße', houseNumber: '1', postalCode: '8600', city: 'Bruck an der Mur' },
    ],
    T: [
        { name: 'Tirol Kliniken — Universitätsklinik Innsbruck', phone: '+43 5 0504 0', street: 'Anichstraße', houseNumber: '35', postalCode: '6020', city: 'Innsbruck' },
        { name: 'Bezirkskrankenhaus Kufstein', phone: '+43 5372 699 0', street: 'Endach', houseNumber: '27', postalCode: '6330', city: 'Kufstein' },
        { name: 'Bezirkskrankenhaus Schwaz', phone: '+43 5224 602 0', street: 'Swarovskistraße', houseNumber: '1', postalCode: '6130', city: 'Schwaz' },
        { name: 'Krankenhaus St. Vinzenz Zams', phone: '+43 5442 600 0', street: 'Sanatoriumstraße', houseNumber: '43', postalCode: '6511', city: 'Zams' },
    ],
    V: [
        { name: 'Landeskrankenhaus Feldkirch', phone: '+43 5522 303 0', street: 'Carinagasse', houseNumber: '47', postalCode: '6800', city: 'Feldkirch' },
        { name: 'Landeskrankenhaus Bregenz', phone: '+43 5574 401 0', street: 'Karl-Pedenz-Straße', houseNumber: '2', postalCode: '6900', city: 'Bregenz' },
        { name: 'Krankenhaus Dornbirn', phone: '+43 5572 303 0', street: 'Lustenauerstraße', houseNumber: '4', postalCode: '6850', city: 'Dornbirn' },
    ],
    K: [
        { name: 'Klinikum Klagenfurt am Wörthersee', phone: '+43 463 538 0', street: 'Feschnigstraße', houseNumber: '11', postalCode: '9020', city: 'Klagenfurt am Wörthersee' },
        { name: 'LKH Villach', phone: '+43 4242 208 0', street: 'Nikolaigasse', houseNumber: '43', postalCode: '9500', city: 'Villach' },
        { name: 'LKH Wolfsberg', phone: '+43 4352 533 0', street: 'Paulitschgasse', houseNumber: '13', postalCode: '9400', city: 'Wolfsberg' },
    ],
    B: [
        { name: 'Krankenhaus der Barmherzigen Brüder Eisenstadt', phone: '+43 2682 600 0', street: 'Esterházystraße', houseNumber: '26', postalCode: '7000', city: 'Eisenstadt' },
        { name: 'Krankenhaus Oberwart', phone: '+43 3352 404 0', street: 'Dornburggasse', houseNumber: '80', postalCode: '7400', city: 'Oberwart' },
        { name: 'Krankenhaus Güssing', phone: '+43 3322 9300 0', street: 'Haydngasse', houseNumber: '2', postalCode: '7540', city: 'Güssing' },
    ],
};
```

- [ ] **Step 2: Commit**

```bash
git add js/hospitals.js
git commit -m "feat: add hospitals data file for all 9 Austrian states"
```

---

## Task 7: Krankenhaus-Selektor UI

**Files:**
- Modify: `index.html`
- Modify: `js/app.js`

- [ ] **Step 1: Bundesland- und Krankenhaus-Selektoren in index.html einfügen**

Im Aufenthalt-Abschnitt (`data-tab="aufenthalt"`), direkt nach `<h2>Krankenhaus</h2>` und **vor** dem bestehenden `<div class="grid-2">` mit Einrichtungsname/Telefon, einfügen:

```html
<div class="grid-2" style="margin-bottom: 12px;">
    <label>
        <span class="label-text">Bundesland</span>
        <select id="org-bundesland">
            <option value="">— Bundesland wählen —</option>
            <option value="W">Wien</option>
            <option value="NOE">Niederösterreich</option>
            <option value="OOE">Oberösterreich</option>
            <option value="S">Salzburg</option>
            <option value="ST">Steiermark</option>
            <option value="T">Tirol</option>
            <option value="V">Vorarlberg</option>
            <option value="K">Kärnten</option>
            <option value="B">Burgenland</option>
        </select>
    </label>
    <label>
        <span class="label-text">Krankenhaus (Quick-Select)</span>
        <select id="org-hospital-select" disabled>
            <option value="">— Bundesland zuerst wählen —</option>
        </select>
    </label>
</div>
```

- [ ] **Step 2: Import in js/app.js ergänzen**

Ganz oben in `js/app.js`, zu den bestehenden Imports, hinzufügen:

```js
import { HOSPITALS_BY_BUNDESLAND } from './hospitals.js';
```

- [ ] **Step 3: setupHospitalSelector()-Funktion in js/app.js hinzufügen**

Vor der `init()`-Funktion einfügen:

```js
function setupHospitalSelector() {
    const blSelect = document.getElementById('org-bundesland');
    const hospSelect = document.getElementById('org-hospital-select');
    if (!blSelect || !hospSelect) return;

    blSelect.addEventListener('change', () => {
        const bl = blSelect.value;
        hospSelect.innerHTML = '<option value="">Krankenhaus auswählen …</option>';
        hospSelect.disabled = !bl;
        if (!bl) return;
        (HOSPITALS_BY_BUNDESLAND[bl] || []).forEach((h, i) => {
            const opt = document.createElement('option');
            opt.value = String(i);
            opt.textContent = h.name;
            hospSelect.appendChild(opt);
        });
    });

    hospSelect.addEventListener('change', () => {
        const bl = blSelect.value;
        const idx = Number(hospSelect.value);
        if (!bl || isNaN(idx) || hospSelect.value === '') return;
        const h = (HOSPITALS_BY_BUNDESLAND[bl] || [])[idx];
        if (!h) return;
        state.organization.name = h.name;
        state.organization.phone = h.phone;
        state.organization.address.street = h.street;
        state.organization.address.houseNumber = h.houseNumber;
        state.organization.address.postalCode = h.postalCode;
        state.organization.address.city = h.city;
        state.organization.address.country = 'A';
        saveState();
        rebindAll();
        setStatus(`Krankenhaus ausgewählt: ${h.name}`);
    });
}
```

- [ ] **Step 4: setupHospitalSelector() in init() aufrufen**

In der `init()`-Funktion, nach `setupPvVisibility();`, hinzufügen:

```js
setupHospitalSelector();
```

- [ ] **Step 5: Browser-Test**

- Aufenthalt-Sektion öffnen
- Bundesland „Salzburg" wählen → Krankenhaus-Dropdown aktiviert sich mit 4 Einträgen
- „Universitätsklinikum Salzburg Landeskrankenhaus" wählen → Felder Name, Telefon, Straße, PLZ, Ort werden ausgefüllt
- „Tirol" wählen → andere Krankenhaus-Liste erscheint
- Felder manuell überschreiben ist weiterhin möglich

- [ ] **Step 6: Commit**

```bash
git add index.html js/app.js
git commit -m "feat: add state-based hospital quick-select with auto-fill"
```

---

## Task 8: Dist-Build und Abschluss

**Files:**
- Modify: `dist/` (via Build-Script)

- [ ] **Step 1: Dist neu bauen**

```bash
bash scripts/build-dist.sh
```

Erwartetes Ergebnis: `dist/` Verzeichnis enthält aktualisierte `index.html`, `css/form.css`, alle `js/`-Dateien inkl. neuem `hospitals.js`.

- [ ] **Step 2: Dist committen**

```bash
git add dist/
git commit -m "chore: rebuild dist for v1.1.0 features"
```

- [ ] **Step 3: Abschluss-Smoke-Test**

Den produktiven Java-Server starten (`./mvnw spring-boot:run`) und aus `dist/index.html` prüfen (oder direkt `index.html`):

1. Version-Badge `v1.1.0` sichtbar, Changelog öffnet sich
2. Faker generiert Adressen aus verschiedenen Bundesländern
3. Ambulant/Stationär-Feld sichtbar, XML-Export zeigt richtigen Code
4. Cloud: „Alle Szenarien" Checkbox → lädt alle ohne Username, zeigt `[username]` Prefix
5. Bundesland → Krankenhaus-Select → Felder werden ausgefüllt

---

## Spec-Coverage-Check

| Spec-Requirement | Task |
|-----------------|------|
| Versionsanzeige oben rechts neben Email Button | Task 1 |
| Changelog (1.0.0 + 1.1.0) | Task 1 |
| Adressdaten für alle Bundesländer | Task 2 |
| Ambulant / Stationär Besuch | Task 3 |
| Alle Cloud-Beispiele laden | Task 4 + 5 |
| Benutzername in Cloud-Liste anzeigen | Task 5 |
| Bundesland-angepasste Krankenhausliste | Task 6 + 7 |
